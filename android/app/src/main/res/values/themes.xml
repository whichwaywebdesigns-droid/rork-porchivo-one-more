<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Splash theme: shown while the app is launching. The splash image is centered
         both horizontally and vertically on a light gray background that matches the
         video frame. -->
    <style name="Theme.RorkApp.Starting" parent="android:Theme.Material.Light.NoActionBar">
        <item name="android:windowBackground">@drawable/splash_background</item>
        <item name="android:windowFullscreen">false</item>
        <item name="android:windowContentOverlay">@null</item>
        <item name="android:windowIsFloating">false</item>
        <item name="android:windowDrawsSystemBarBackgrounds">true</item>
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>
    </style>

    <!-- Main app theme, applied after the splash drawable. -->
    <style name="Theme.RorkApp" parent="android:Theme.Material.Light.NoActionBar">
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>
    </style>
</resources>
